{
  "name": "chorus-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm ci && npm run build",
    "start": "cd web && npm run preview"
  },
  "dependencies": {
    "web": "file:./web"
  }
}
